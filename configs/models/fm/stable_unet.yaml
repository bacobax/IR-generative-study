# FM UNet architecture – stable variant (current default)
# This mirrors fm_src/stable_unet_config.json
block_out_channels: [128, 256, 512]
down_block_types: ["DownBlock2D", "AttnDownBlock2D", "DownBlock2D"]
up_block_types: ["UpBlock2D", "AttnUpBlock2D", "UpBlock2D"]
in_channels: 4
out_channels: 4
layers_per_block: 2
sample_size: 64
