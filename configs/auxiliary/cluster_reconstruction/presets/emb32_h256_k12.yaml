# Cluster reconstruction: emb32, hidden256, k12
# Launcher: scripts/train/cluster_emb32_h256_k12.sh
# Only values that differ from train_cluster_reconstruction.py defaults are listed.
k_regions: 12
device: cuda:3
run_name: emb32_hidden256_proj_mask50_k12
