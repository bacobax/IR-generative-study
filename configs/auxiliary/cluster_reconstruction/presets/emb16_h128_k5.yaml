# Cluster reconstruction: emb16, hidden128, k5
# Launcher: scripts/train/cluster_emb16_h128_k5.sh
# Only values that differ from train_cluster_reconstruction.py defaults are listed.
emb_dim: 16
hidden_dim: 128
device: cuda:0
run_name: emb16_hidden128_proj_mask50_k5
