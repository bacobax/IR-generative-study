# Unconditional latent Flow Matching OT on the 5.1k tile-balanced BigEarthNet B08 subset.
extends: fm_scratch_ben_uncond_full.yaml

data:
  subset_manifest: "train_5100_tile_balanced.json"

training:
  gradient_accumulation_steps: 1

output:
  model_dir: "./artifacts/checkpoints/flow_matching/serious_runs/fm_scratch_ben_uncond_5k"
  log_dir: "./artifacts/runs/main/flow_matching/fm_scratch_ben_uncond_5k"
  debug_dir: "./artifacts/debug/fm_scratch_ben_uncond_5k"
