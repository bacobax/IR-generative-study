# Unconditional latent Flow Matching OT on the 2.04k tile-balanced BigEarthNet B08 subset.
extends: fm_scratch_ben_uncond_full.yaml

data:
  subset_manifest: "train_2040_tile_balanced.json"

training:
  gradient_accumulation_steps: 1

output:
  model_dir: "./artifacts/checkpoints/flow_matching/serious_runs/fm_scratch_ben_uncond_2k"
  log_dir: "./artifacts/runs/main/flow_matching/fm_scratch_ben_uncond_2k"
  debug_dir: "./artifacts/debug/fm_scratch_ben_uncond_2k"
  resume: "latest"
